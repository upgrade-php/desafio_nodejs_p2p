const health_router = require('./health')
const person_router = require('./person')
const pay_router = require('./pay')

module.exports = {
  health_router: health_router,
  person_router: person_router,
  pay_router: pay_router,
}
